version: 2

sources:
  - name: salesforce
    database: bq-dataworkeragent-test
    schema: salesforce_raw
    tables:
      - name: Contract
        description: "Raw Salesforce Contract data ingested via BQ DTS"
      - name: AccountFeed
      - name: AccountContactRole
